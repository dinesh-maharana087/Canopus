# Root configuration index. Component-specific examples live in their component directories.
# Production values are required explicitly and are never populated by this file.
# See agent/.env.example and deploy/.env.prod.example for variable shape only.
# Do not copy this file as a live environment or place credentials in the repository.
