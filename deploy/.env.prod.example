# Production deployment values. Replace every placeholder before use.
DEVICE_WATCH_DOMAIN=watch.example.invalid
DATABASE_URL=mysql+pymysql://user:password@db.example.invalid/device_watch?ssl_ca=/run/secrets/mysql-ca.pem&ssl_verify_cert=true&ssl_verify_identity=true
MYSQL_CA_CERT_PATH=./secrets/mysql-ca.pem
