{$DEVICE_WATCH_DOMAIN} {
    encode zstd gzip

    handle /api/* {
        reverse_proxy server:8000
    }

    handle {
        root * /srv
        try_files {path} /index.html
        file_server
    }
}
