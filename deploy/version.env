# Device Health Portal - Production Build Versions
# Do not store secrets in this file.

PYTHON_VERSION=3.12.6
UV_VERSION=0.12.13

# Frontend build toolchain
# Use >=22.22.0 because the current frontend dependency set requires it.
NODE_VERSION=22.22.0
NPM_VERSION=10.9.2

# Database family
MYSQL_MAJOR_VERSION=8

# Application environment
DEVICE_WATCH_ENV=production